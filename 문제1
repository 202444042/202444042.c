#include <stdio.h>

int main()
{
    int num=1;
    
    (?) (num!=0)
    #include <stdio.h>

int main()
{
    int num=1;
    
    (?) (num!=0)
    //?안에 들어가야 하는것은?
    {
        printf("숫자를 입력하세요 : ");
        scanf("%d",&num);
        
        if(num%2==0&&num!=0)
        {
            printf("%d는 짝수입니다.\n",num);
        }
        if(num%2!=0)
        {
            printf("%d는 홀수입니다.\n",num);
        }
        if(num==0)
#include <stdio.h>

int main()
{
    int num=1;
    
    (?) (num!=0)
    //?안에 들어가야 하는것은?
    {
        printf("숫자를 입력하세요 : ");
        scanf("%d",&num);
        
        if(num%2==0&&num!=0)
        {
            printf("%d는 짝수입니다.\n",num);
        }
        if(num%2!=0)
        {
            printf("%d는 홀수입니다.\n",num);
        }
        if(?)
        //?안에 들어가야 하는것은?
        {
            printf("숫자가 0이므로 종료합니다.\n");
        }
    }

    return 0;
}
        {
            printf("숫자가 0이므로 종료합니다.\n");
        }
    }

    return 0;
}
    {
        printf("숫자를 입력하세요 : ");
        scanf("%d",&num);
        
        if(num%2==0&&num!=0)
        {
            printf("%d는 짝수입니다.\n",num);
        }
        if(num%2!=0)
        {
            printf("%d는 홀수입니다.\n",num);
        }
        if(num==0)
        {
            printf("숫자가 0이므로 종료합니다.\n");
        }
    }

    return 0;
}
